int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    mt19937 rng(chrono::steady_clock::now().time_since_epoch().count());
    cout << fixed << showpoint << setprecision(13);

    int num_cases;
    cin >> num_cases;
    for (int case_num = 1; case_num <= num_cases; case_num++) {

    }

    return 0;
}
