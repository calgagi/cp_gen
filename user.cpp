void solve() {

    return;
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    srand(chrono::steady_clock::now().time_since_epoch().count());

    int tt = 1;
    /* cin >> tt; */
    while (tt--) {
        solve();
    }

    return 0;
}
